[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
	# deploy_dir = deploy
	# revert_dir = revert
	# verify_dir = verify
	# extension = sql
# [core "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "forward-api"]
	uri = db:pg:forward-api
[core "pg"]
	target = forward-api
